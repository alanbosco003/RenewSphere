import 'package:flutter/material.dart';

class Collateral extends StatelessWidget {
  const Collateral({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
        child: Scaffold(
      body: Center(
        child: Text("Hi My colletreal page"),
      ),
    ));
  }
}
